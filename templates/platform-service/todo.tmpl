# {{.ProjectName}} — todo

- [ ] Adjust the `item` domain (or replace it) to model your real entity.
- [ ] Add domain validation rules in models/*.go .Validate().
- [ ] Wire any new domains into internal/di and internal/server.
